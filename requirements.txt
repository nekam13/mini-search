# Mini Search - Production Dependencies
# Optimized for Ubuntu 26.04 ARM64 + Termux environment
# Lightweight version without heavy compilation dependencies

# Web Framework
Flask==3.0.3
Werkzeug==3.0.3

# Scheduling
APScheduler==3.10.4
pytz==2024.1

# HTTP & Parsing
requests==2.31.0
beautifulsoup4==4.12.2
lxml==5.2.1
feedparser==6.0.10
extruct==0.15.0

# Utilities
urllib3==2.2.2

# Vector search alternatives (lighter options)
# Option 1: ChromaDB (try latest version with better ARM64 support)
# chromadb>=0.5.0

# Option 2: Lightweight alternative (uncomment if ChromaDB fails)
# hnswlib==0.8.0
# numpy==1.26.4
